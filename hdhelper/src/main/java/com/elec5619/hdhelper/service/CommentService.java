package com.elec5619.hdhelper.service;

import com.elec5619.hdhelper.entity.Comment;
import com.elec5619.hdhelper.entity.Course;
import com.elec5619.hdhelper.entity.Student;

import java.util.List;

public interface CommentService {
//    Comment saveComment(Comment comment);
//    List<Comment> findByCourse(Course course);
//    List<Comment> findByStudent(Student student);
    // One student can only comment once in one course
//    Comment findByCourseAndStudent(Course course, Student student);

}
